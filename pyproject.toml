[project]
name = "hermes-code-agent"
version = "2.4.0"
description = "Self-correcting coding workflow as a Hermes skill (stdlib-only gate script)."
requires-python = ">=3.8"

[tool.pytest.ini_options]
testpaths = ["tests"]
