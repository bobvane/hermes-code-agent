# AGENTS.md — project rules for Hermes Code Agent

Drop this file at your repo root. The `hermes-code-agent` skill reads it as optional project context.

## Stack & tooling
- Language / framework:
- Package manager:
- Test command:
- Lint command:
- Build command:

## Hard rules (agent must obey)
- Never commit secrets or `.env`.
- Never use `force` push to `main`/`master`.
- Run tests before reporting done.

## Conventions
- Naming:
- File layout:
- Commit message style:

## Out of scope
- Things the agent should NOT touch:

<!-- Edit freely. Empty sections are fine; the skill works without this file. -->
